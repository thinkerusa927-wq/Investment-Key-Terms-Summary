---
name: create-investment-key-terms-summary
description: 仅以用户在本次调用中提供的最终版投资/融资交易协议（包括SSA、SHA、SPA、股份认购/购买协议及股东协议）为资料来源，提取、核验并生成面向CEO或管理层的中文Key Terms Summary（交易文件核心条款总结）。用于需要复刻既有Word格式、同时覆盖本次提供的多份文件、按项目筛选真正影响交易经济、控制、业务合作和退出的条款，并以简洁商业语言呈现的任务。
---

# 投资项目 Key Terms Summary

## 必须加载的资源

1. 先完整阅读`references/Key Terms Summary格式参考.md`。
2. 再完整阅读`references/正文内容风格参考.md`。
3. 需要生成Word时，以`assets/Key Terms Summary模板.docx`为默认设计权威。
4. DOCX输入或输出须配合文档处理技能执行“渲染-逐页检查-迭代”。

## 输入范围

- 用户在本次调用中明确提供的交易文件，是本次任务唯一、最终且完整的资料来源。
- 只提供一份文件时，直接提取、核验并总结，不得寻找其他协议佐证或补充。
- 同时提供多份最终文件时，只在这些文件之间核对金额、股权、治理、退出等安排是否一致，不得把核对范围扩大到未提供文件。
- 只有用户明确要求版本比较或指定额外文件时，才读取并比较该等文件。
- supporting references仅提供格式、写作和内容筛选方法，不得据此扩大资料范围。

## 工作流

### 1. 识别本次文件及其角色

- 列出本次提供的每份文件的类型、当事方、签署状态及交易功能。
- 区分SSA/SPA中的交易及交割安排与SHA中的持续治理安排。
- 本次提供的多份文件之间存在冲突时，仅在冲突影响准确总结时向用户说明。

### 2. 全文提取并建立条款矩阵

- 逐页提取本次提供文件的全文，不依赖目录或关键词抽样。
- 同时渲染并检查每页，确认签署页、附件、表格及页眉页脚没有被漏读。
- 在工作底稿中为每个候选条款记录：文件、条款号/页码、权利或义务主体、触发条件、期限、金额/比例/门槛、结果或救济、与本次提供的其他最终文件的关系。
- 条款来源矩阵默认为任务内部复核底稿，不写入CEO摘要，也不作为默认交付物；用户要求可追溯版本时再另行交付。
- 只有一份文件时，在该文件内部完成数字、定义和条款引用核验；有多份文件时，仅在本次文件之间进行一致性核对。
- 至少在底稿中复核投资金额、估值、股数、持股比例、董事席位、否决权门槛、转让机制、回购/清算价格、仲裁机构及语言。完成复核不代表这些内容都要进入CEO摘要。

### 3. 形成CEO版正文

- 按`references/正文内容风格参考.md`先筛选、再改写。全文核验追求完整，CEO正文不追求条款类别完整。
- 正文按SSA、SPA、SHA等文件分组并依次排列，每组内再按交易经济、项目合作、治理、退出和创始人承诺等主题组织；通过事项名称中的`（SSA）`、`（SPA）`或`（SHA）`标明来源，不另设合并三列的协议分组行。事项数量由项目决定。
- 候选条款只有满足至少一项时才进入正文：改变钱、股权、控制或退出结果；体现本项目特有的业务/合作安排；显著偏离市场惯例；直接影响是否签署或如何落地；承载本项目最重要的商业预期。
- 惯常条款默认不写，例如普通转让限制、通知、优先购买、优先认购、交割程序、争议解决、一般违约及保密等。分红权、知情权和反稀释权属于例外：协议有约定时合并为一句结果性概括，协议未约定的权利不补写。其他惯常条款如有明显不惯常的具体表述，向用户说明并询问是否属于需要写入摘要的特别约定。
- 项目特有的核心业务边界、区域合作、合资比例、并表安排、资源投入或创始人承诺，通常比惯常投资人权利更值得进入摘要。
- 只写商业结果和真正有区分度的数字。一般不展开通知方式、行权步骤、回复/付款窗口、补救期、交付物、例外层级或条款号；只有它们会改变权利价值、现金回收或签署判断时才保留。
- 核心交易事项可用2至3个短句；其他事项原则上1个短句，必要时最多2个。单句优先采用“短标签：商业事实/结果”，不为满足形式而塞齐主体、触发、期限和救济。
- 以协议文本为准；协议未直接写明但可以推算的数据，以及日期、主体、地址、数字等空白事项，均不得写入最终Word。本次文件之间的冲突先在底稿记录，仅在用户要求或直接影响准确总结时向用户说明。

### 4. 生成DOCX

用户仅要求提纲、试跑或文本预览时，可跳过DOCX生成与渲染，但须明确标注「未进行最终版式/视觉验收」；一旦交付DOCX，仍须执行第5步。

优先把结构化内容写入JSON并运行：

```powershell
python scripts/build_key_terms_summary.py --input-json input.json --output output.docx
```

JSON结构：

```json
{
  "title": "项目名称 - 交易文件核心条款总结",
  "date": "YYYY.MM.DD",
  "rows": [
    {
      "label": "投资、股权（SSA）",
      "items": [
        {"lead": "我方投资：", "body": "US$...认购...股，交割后持股...%。"}
      ]
    }
  ]
}
```

- 使用Codex工作区依赖提供的Python环境，不依赖系统Python。
- 不得修改技能内模板原件；脚本必须复制模板后再编辑。
- 如用户提供控制格式的DOCX，先按文档模板蒸馏流程建立任务内格式契约，再从该参考副本制作。

### 5. 篇幅与视觉校验

- 目标为1至1.5页，硬上限为2页；页数只是结果，不是决定主题数量或单项长度的依据。
- 先检查每个事项是否值得出现，再把单项压到CEO一眼能读完。优先整项删除低价值惯常条款，其次删除程序、条件细节和法言法语，再合并重复机制；不得靠缩到不可读字号硬塞。
- 将最终DOCX渲染为逐页PNG并在100%比例检查：表格边框、换页、重复表头、字体、行距、数字断行、留白及中文字符。
- 任何实质修改后重新渲染；未通过视觉检查不得交付。

## 交付门槛

- 所有本次提供的文件均已全文读取；存在多份文件时，仅在这些文件之间完成一致性核对。
- 未主动搜索、读取或引用本次输入范围之外的项目文件。
- 所有关键数字可回溯到原文；估值、股本和投资额没有混用。
- 交割条件、否决权、转让机制、反稀释、回购和清算已在底稿中核验；摘要只呈现其中对本项目有决策增量的部分，未写惯常条款不视为遗漏。
- 文本矛盾已在底稿复核；只有用户要求或直接影响签署/决策时才克制提示，但无需写入摘要。
- 最终页数不超过2页，视觉检查无缺陷。
- 默认只交付最终DOCX；参考资料和渲染中间件不作为用户交付物，除非用户另有要求。
